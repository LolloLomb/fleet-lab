apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: opensearch-dashboard
  namespace: fleet-local
spec:
  repo: "https://github.com/LolloLomb/fleet-lab"
  branch: main
  paths:
    - opensearch-dashboard
