#tolerations:
#  - key: node/gaia
#    operator: Equal
#    value: "true"
#    effect: NoSchedule
#nodeSelector:
#  node-type: baremetal
replicaCount: 1

opensearchHosts: "http://opensearch-cluster-master:9200"

resources:
  requests:
    cpu: "500m"
    memory: "1Gi"
  limits:
    cpu: "1"
    memory: "1Gi"

service:
  type: ClusterIP

config:
  opensearch_dashboards.yml: |
    server.host: "0.0.0.0"
    server.port: 5601

    opensearch.hosts:
      - "http://opensearch-cluster-master:9200"

    opensearch.ssl.verificationMode: none

    opensearch.username: "admin"
    opensearch.password: "V7!qR2#zLm9@Kp4"

    opensearch.requestHeadersWhitelist:
      - authorization
      - securitytenant

    opensearch_security.multitenancy.enabled: true
    opensearch_security.multitenancy.tenants.preferred:
      - Private
      - Global
