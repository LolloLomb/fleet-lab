#tolerations:
#  - key: node/gaia
#    operator: Equal
#    value: "true"
#    effect: NoSchedule
#nodeSelector:
#  node-type: baremetal
replicaCount: 1

opensearchHosts: "https://opensearch-cluster-master:9200"

opensearchAccount:
  secret: opensearch-dashboards-account

service:
  type: ClusterIP

config:
  opensearch_dashboards.yml:
    opensearch.ssl.verificationMode: none # cambiare
    opensearch_security.multitenancy.enabled: true
    opensearch_security.multitenancy.tenants.preferred: Private
    server.host: "0.0.0.0"
