metrics:
  enabled: true
postgres:
  enabled: false
database:
  type: "postgres"
  host: "postgres.keycloak.svc.cluster.local"
  existingSecret: "keycloak-db-credentials"
  secretKeys:
    passwordKey: "password"
    usernameKey: "username"
### aggiungere nodeSelector e tolerations
